import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.bingzingping.snakegame',
  appName: 'SnakeGame',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
